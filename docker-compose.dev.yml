services:
  # -----------------------------------
  # 1. DATABASE SERVICE (PostgreSQL)
  # -----------------------------------
  db:
    image: postgres:16-alpine
    container_name: postgres_pickard_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER} 
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD} 
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # ports:
    #   - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d thornton_pickard"]
      interval: 5s
      timeout: 5s
      retries: 5

  # -----------------------------------
  # 2. GO GIN API SERVICE
  # -----------------------------------
  api:
    build:
      context: ../Thornton-Pickard-Api
      dockerfile: Dockerfile
    container_name: go_api_server
    image: thornton-pickard-api:latest
    ports:
      - "8080:8080"
      
    depends_on:
      db:
        condition: service_healthy 
      
    environment:
      DB_HOST: db           
      DB_PORT: 5432
      DB_USER: ${POSTGRES_USER}
      DB_NAME: ${POSTGRES_DB}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      APP_SERVICE_NAME: ${APP_SERVICE_NAME}
    
  # -----------------------------------
  # 3. REACT UI SERVICE
  # -----------------------------------
  ui:
    build:
      context: ../Thornton-Pickard-Ui
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    environment:
      - VITE_APP_API_URL=http://localhost:8080/api/v1
    command: npm run dev
    depends_on:
      - api
    volumes:
      - ../Thornton-Pickard-Ui/src:/app/src
      - ../Thornton-Pickard-Ui/public:/app/public
      - ../Thornton-Pickard-Ui/index.html:/app/index.html
      - ../Thornton-Pickard-Ui/vite.config.ts:/app/vite.config.ts
      - ../Thornton-Pickard-Ui/tailwind.config.js:/app/tailwind.config.js
      - ../Thornton-Pickard-Ui/tsconfig.json:/app/tsconfig.json
      - ../Thornton-Pickard-Ui/tsconfig.app.json:/app/tsconfig.app.json
      - ../Thornton-Pickard-Ui/tsconfig.node.json:/app/tsconfig.node.json
      - ../Thornton-Pickard-Ui/eslint.config.js:/app/eslint.config.js
      - ../Thornton-Pickard-Ui/.prettierrc:/app/.prettierrc
      - ../Thornton-Pickard-Ui/.env:/app/.env
      - /app/node_modules